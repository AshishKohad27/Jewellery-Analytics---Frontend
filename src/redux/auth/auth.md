Login → Save tokens → Attach token globally → Call APIs → Token expires → Refresh → Continue → Use role for access

User logs in
↓
Tokens saved
↓
Axios header set globally
↓
User navigates app
↓
API calls authenticated
↓
Access token expires
↓
Refresh token used
↓
New access token saved
↓
User continues
↓
Role used to control UI access
